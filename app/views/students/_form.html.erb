<%= form_for(@student) do |form| %>
  <ul>
    <% @student.errors.full_messages.each do |error|%>
      <li><%= error %></li>
      <% end %>
  </ul>
  <p>
    <%= form.label :name %><br />
    <%= form.text_field :name %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
