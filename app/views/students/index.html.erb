<h1>All Students</h1>

<% @students.each do |student| %>
  <ul>
    <li><%= link_to student.name, student_path(student), class:'student-name'%></li>
    <li><%= student.name %></li>
  </ul>
  <% end %>

<%= link_to "Add New Student", new_student_path, class: 'new-student' %>
