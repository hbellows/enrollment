<h1>All Students</h1>

<% @students.each do |student| %>
  <ul>
    <li><%= link_to student.name, student_path(student), class:'student-name'%></li>
    <li><%= student.name %></li>
  </ul>
  <% end %>
